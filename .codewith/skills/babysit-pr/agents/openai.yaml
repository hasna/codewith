interface:
  display_name: "PR Babysitter"
  short_description: "Watch PR review comments, CI, and merge conflicts"
  default_prompt: "Use $babysit-pr to watch the current PR, surface review and CI changes, fix branch-caused issues, and keep monitoring until a strict stop condition."
