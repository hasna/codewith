name: iapp-codex-cli

on:
  push:
    branches:
      - "feat/loop-scheduled-tasks"
    paths:
      - "codex-rs/**"
      - ".github/workflows/iapp-codex-cli.yml"
      - "scripts/iapp-codex"
      - "justfile"
  pull_request:
    paths:
      - "codex-rs/**"
      - ".github/workflows/iapp-codex-cli.yml"
      - "scripts/iapp-codex"
      - "justfile"
  workflow_dispatch:
    inputs:
      profile:
        description: "Cargo profile to build for the downloadable iapp-codex artifact."
        type: choice
        required: true
        default: release
        options:
          - release
          - debug

permissions:
  contents: read

jobs:
  build-linux:
    name: Build iapp-codex Linux CLI
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    defaults:
      run:
        working-directory: codex-rs
    env:
      BUILD_PROFILE: ${{ github.event_name == 'workflow_dispatch' && inputs.profile || 'debug' }}
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - name: Install Linux build prerequisites
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            libcap-dev \
            libssl-dev \
            pkg-config

      - uses: dtolnay/rust-toolchain@a0b273b48ed29de4470960879e8381ff45632f26 # 1.93.0

      - name: Build codex CLI
        shell: bash
        run: |
          set -euo pipefail

          cargo_args=(build --locked -p codex-cli --bin codex)
          if [[ "${BUILD_PROFILE}" == "release" ]]; then
            cargo_args+=(--release)
          fi
          cargo "${cargo_args[@]}"

      - name: Stage iapp-codex artifact
        id: stage
        shell: bash
        run: |
          set -euo pipefail

          profile_dir=debug
          if [[ "${BUILD_PROFILE}" == "release" ]]; then
            profile_dir=release
          fi

          stage_dir="dist/iapp-codex-linux-x86_64-${BUILD_PROFILE}"
          archive="dist/iapp-codex-linux-x86_64-${BUILD_PROFILE}.tar.gz"
          rm -rf "${stage_dir}" "${archive}"
          mkdir -p "${stage_dir}"
          install -m 0755 "../scripts/iapp-codex" "${stage_dir}/iapp-codex"
          install -m 0755 "target/${profile_dir}/codex" "${stage_dir}/iapp-codex-bin"
          "${stage_dir}/iapp-codex" --version
          tar -C "${stage_dir}" -czf "${archive}" iapp-codex iapp-codex-bin

          echo "archive=codex-rs/${archive}" >> "$GITHUB_OUTPUT"

      - name: Upload iapp-codex artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: iapp-codex-linux-x86_64-${{ env.BUILD_PROFILE }}
          path: ${{ steps.stage.outputs.archive }}
