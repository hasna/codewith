{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "authProfile": {
      "default": null,
      "description": "Auth profile used for this read. `null` selects the default root auth; omission leaves the app-server's selected auth profile unchanged.",
      "type": [
        "string",
        "null"
      ]
    },
    "includeResetCreditDetails": {
      "description": "Include detailed reset-credit rows even when no credits remain available.",
      "type": "boolean"
    }
  },
  "title": "GetAccountRateLimitsParams",
  "type": "object"
}