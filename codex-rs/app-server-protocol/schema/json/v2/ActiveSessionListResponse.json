{
  "$defs": {
    "AbsolutePathBuf": {
      "description": "A path that is guaranteed to be absolute and normalized (though it is not\nguaranteed to be canonicalized or exist on the filesystem).\n\nIMPORTANT: When deserializing an `AbsolutePathBuf`, a base path must be set\nusing [AbsolutePathBufGuard::new]. If no base path is set, the\ndeserialization will fail unless the path being deserialized is already\nabsolute.",
      "type": "string"
    },
    "ActiveSessionCapability": {
      "enum": [
        "receiveMessage",
        "queueMessage",
        "triggerTurn",
        "claudeChannelBridge"
      ],
      "type": "string"
    },
    "ActiveSessionPeer": {
      "properties": {
        "agentPath": {
          "type": [
            "string",
            "null"
          ]
        },
        "authProfile": {
          "default": null,
          "description": "Auth profile selected for model requests in this active peer, if known.",
          "type": [
            "string",
            "null"
          ]
        },
        "authProfileKind": {
          "$ref": "#/$defs/AuthProfileKind",
          "default": "unknown",
          "description": "Whether `authProfile` is the default root profile or a named profile."
        },
        "capabilities": {
          "default": [],
          "items": {
            "$ref": "#/$defs/ActiveSessionCapability"
          },
          "type": "array"
        },
        "cwd": {
          "$ref": "#/$defs/AbsolutePathBuf"
        },
        "displayName": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/$defs/ActiveSessionPeerKind",
          "default": "codewithSession"
        },
        "lastSeenAt": {
          "format": "int64",
          "type": "integer"
        },
        "peerId": {
          "default": "",
          "description": "Active peer routing key returned by activeSession/list. For local\nCodewith sessions this matches thread_id; bridge peers may use a\ntransport-scoped id.",
          "type": "string"
        },
        "sessionId": {
          "default": "",
          "description": "Live session instance id for the loaded thread. This is not a durable\nmailbox or remote machine id.",
          "type": "string"
        },
        "threadId": {
          "description": "Codewith thread that owns the active peer registration.",
          "type": "string"
        }
      },
      "required": [
        "threadId",
        "cwd",
        "lastSeenAt"
      ],
      "type": "object"
    },
    "ActiveSessionPeerKind": {
      "enum": [
        "codewithSession",
        "spawnedAgent",
        "bridgeAdapter"
      ],
      "type": "string"
    },
    "AuthProfileKind": {
      "oneOf": [
        {
          "const": "unknown",
          "description": "The source did not record auth profile selection.",
          "type": "string"
        },
        {
          "const": "default",
          "description": "The default root auth profile was selected explicitly or by runtime default.",
          "type": "string"
        },
        {
          "const": "named",
          "description": "A named auth profile was selected.",
          "type": "string"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "data": {
      "items": {
        "$ref": "#/$defs/ActiveSessionPeer"
      },
      "type": "array"
    },
    "nextCursor": {
      "description": "Opaque cursor to pass to the next call to continue after the last item.\nif None, there are no more items to return.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "data"
  ],
  "title": "ActiveSessionListResponse",
  "type": "object"
}