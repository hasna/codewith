{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ThreadMonitorRouting": {
      "enum": [
        "stream",
        "file",
        "both"
      ],
      "type": "string"
    }
  },
  "properties": {
    "command": {
      "type": "string"
    },
    "cwd": {
      "description": "Optional working directory relative to the thread cwd. Parent directory traversal and absolute paths are rejected.",
      "type": [
        "string",
        "null"
      ]
    },
    "name": {
      "type": "string"
    },
    "outputFile": {
      "description": "Optional output file relative to the monitor cwd. Parent directory traversal, absolute paths, and symlink targets are rejected.",
      "type": [
        "string",
        "null"
      ]
    },
    "prompt": {
      "type": "string"
    },
    "routing": {
      "anyOf": [
        {
          "$ref": "#/definitions/ThreadMonitorRouting"
        },
        {
          "type": "null"
        }
      ]
    },
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "command",
    "name",
    "prompt",
    "threadId"
  ],
  "title": "ThreadMonitorCreateParams",
  "type": "object"
}